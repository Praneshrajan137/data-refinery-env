claims:
  - claim: csv_verified_repair
    status: beta
    evidence: Local CSV lifecycle tests exercise detect, propose, safety, SMT verify, dry-run/apply, audit, and byte-identical revert.
  - claim: duckdb_verified_patch_plan
    status: beta
    evidence: DuckDB table-store tests cover patch-plan hashing, preflight row-count guards, transaction journaling, and rollback SQL.
  - claim: dbt_aware_receipts
    status: beta
    evidence: Core dbt manifest tests map generic tests into DataForge constraints; docs/evidence/dbt_duckdb/fresh_env_report.json proves the published dataforge_07_dbt package through dbt seed/run/test, dry-run/apply/refuse, audit, and revert in a fresh PyPI install.
  - claim: mcp_integration
    status: beta
    evidence: MCP stdio tests expose dry-run repair receipts and keep apply behind explicit tool enablement.
  - claim: public_repair_receipt_contract
    status: beta
    evidence: CLI, playground, and MCP repair receipts expose schema_version, receipt_version, contract_version, source hashes, candidate repairs, proof obligations, root causes, limitations, and revert metadata.
  - claim: smt_verified_supported_constraints
    status: beta
    evidence: Unit tests cover type, domain, functional dependency, not-null, unique, accepted-values, and regex verifier outcomes.
  - claim: constitutional_safety_layer
    status: beta
    evidence: Safety tests enforce PII, row-delete, primary-key edit, aggregate-sensitive, high-volume, prompt-injection text, LLM-originated, and conflicting-write policy gates.
  - claim: reversible_transactions
    status: beta
    evidence: Transaction tests assert source snapshots, stale-source refusal, atomic apply, audit, and byte-identical revert.
  - claim: credentialed_cloud_warehouse_apply
    status: roadmap
    evidence: Cloud warehouse mutation remains a dry-run contract boundary until real credentialed conformance suites pass.
  - claim: production_autonomous_repair
    status: roadmap
    evidence: Mutations require explicit apply approval; background autonomous cloud repair is not claimed.
  - claim: design_partner_validation
    status: roadmap
    evidence: CONTRIBUTORS.md marks the design-partner gate as not met.
  - claim: final_dataforge07_pypi_distribution
    status: shipped
    evidence: docs/evidence/pypi/publish_report.json records PyPI/TestPyPI publication, Trusted Publishing attestations, artifact hashes, provenance URLs, and fresh-install smoke logs for dataforge_07, dataforge_07_mcp, dataforge_07_evals, dataforge_07_dbt, and dataforge_07_agent_patterns.
  - claim: workers_dev_playground
    status: beta
    evidence: The full-vision gate verifies https://dataforge.praneshrajan15.workers.dev/playground serves the Cloudflare frontend, points at the expected HF backend, and passes live repair-flow checks.
  - claim: trained_model_family
    status: roadmap
    evidence: docs/evidence/ledger.json is the canonical claim index; public leaf-verifier evidence covers 0.5B SFT plus 0.5B GRPO research evidence, 0.5B-GRPO v2 is preserved as quality_gate_failed_no_upload diagnostic evidence, SFT-v5/v6/v7/v8 are failed or blocked private diagnostic lineages, SFT-v9 is only a private action-envelope curriculum/preflight candidate whose first Kaggle smoke failed before training on a P100 capability guard and whose fixed smoke v2 is running, GRPO-v4 is blocked until SFT-v9-or-later has a promoted private checkpoint, and larger SFT/GRPO/GiGPO plus full-family quality claims remain blocked.
