# Declared premise for the curated Hospital playground sample.
#
# GENERATED by scripts/playground/build_samples.py. Do not hand-edit:
# tests/integration/test_playground_smoke.py pins this file to the generator.
#
# `er_wait_time` (int) and `rating` (float) are DISCRIMINATING types, so they confer write
# authority and an external fix to them can be proven. `str` columns are inert by design --
# see dataforge.engine.repair.authoritative_columns and domain.vocabulary.type_discriminates.
#
# No functional dependency is declared: provider_number -> hospital_name is FALSE in this
# sample (PRV001 has two names), and declaring a refuted dependency to dress up a demo is
# exactly the kind of premise inflation this project measures against.

columns:
  provider_number: str
  hospital_name: str
  city: str
  state: str
  zip_code: str
  phone_number: str
  rating: float
  mortality_rate: float
  readmission_rate: float
  er_wait_time: int
